"use strict";
/*import prisma from './client'

async function resetData() {
  try {
    console.log("🧹 Limpando dados do banco (exceto usuários)...");

    // Exemplo de tabelas — substitua pelos nomes reais do seu schema
    await prisma.transferencia.deleteMany({});
    await prisma.abastecimento.deleteMany({});
    await prisma.tanque.deleteMany({});
    

    console.log("✅ Dados resetados com sucesso (tabela User preservada).");
  } catch (error) {
    console.error("❌ Erro ao limpar dados:", error);
  } finally {
    await prisma.$disconnect();
  }
}

resetData();*/
